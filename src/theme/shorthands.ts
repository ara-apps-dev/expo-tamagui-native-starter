export const shorthands = {
  textVariants: {
    heading: {
      fontFamily: "$body",
      fontSize: "$headline-lg",
      fontWeight: "$regular",
      lineHeight: "$headline-lg",
    },
    body: {
      fontFamily: "$body",
      fontSize: "$body-md",
      fontWeight: "$regular",
      lineHeight: "$body-md",
    },
    label: {
      fontFamily: "$body",
      fontSize: "$label-sm",
      fontWeight: "$regular",
      lineHeight: "$label-sm",
    },
  },
};
